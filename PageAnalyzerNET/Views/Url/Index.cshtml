@using Microsoft.AspNetCore.Mvc.TagHelpers
@model PageAnalyzerNET.ViewModels.IndexViewModel

<div class="container-lg mt-5">
    <table class="table table-bordered table-hover mt-3">
        <thead>
        <tr>
            <th class="col-1">ID</th>
            <th>Имя</th>
            <th class="col-2">Последняя проверка</th>
            <th class="col-1">Код ответа</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var url in Model.Urls)
        {
            <tr>
                <td>@url.Id</td>
                <td>
                    <a asp-action="Show" asp-controller="Url" asp-route-id="@url.Id">@url.Name</a>
                </td>
                <td>@url.CreatedAt</td>
                <td>LastCheck</td>
            </tr>
        }
        </tbody>
    </table>

    <page-link page-model="Model.PageViewModel" page-action="Index"></page-link>
    @* <nav aria-label="Page navigation"> *@
    @*     <ul class="pagination justify-content-center mt-5"> *@
    @*         @if (Model.PageViewModel.HasPreviousPage) *@
    @*         { *@
    @*             <li> *@
    @*                 <a asp-action="Index" asp-controller="Url" asp-route-page="@(Model.PageViewModel.PageNumber - 1)"  *@
    @*                    class="page-link">Назад</a> *@
    @*             </li> *@
    @*         } *@
    @*         <li> *@
    @*             <a class="page-item">@Model.PageViewModel.PageNumber</a> *@
    @*         </li> *@
    @*         @if (Model.PageViewModel.HasNextPage) *@
    @*         { *@
    @*             <li> *@
    @*                 <a asp-action="Index" asp-controller="Url" asp-route-page="@(Model.PageViewModel.PageNumber + 1)"  *@
    @*                    class="page-link">Вперед</a> *@
    @*             </li>   *@
    @*         } *@
    @*     </ul> *@
    @* </nav> *@
</div>